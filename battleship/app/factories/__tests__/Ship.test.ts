import Ship, { IthisShip } from "../Ship";

describe("Ship factory function", () => {
	it('test hit functionality',()=>{
		// // Arrange->mock the this object
		// beforeEach((){
		// 	let thisObj:IthisShip={length:3,player:'you',hits:0}
		// })
		// let ship=Ship(thisObj,3,thisObj.player)

		// // Act
		// ship.hit.call(thisObj)
	})
});
